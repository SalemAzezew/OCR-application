[build]
  publish = "."  # The root directory where your HTML file is located
  command = "npm install && npm run build"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/upload.html"
  status = 200

